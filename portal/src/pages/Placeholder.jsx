const Placeholder = ({ title }) => (
  <div>
    <h1 className="text-xl font-semibold mb-2">{title}</h1>
    <p className="text-sm text-subtle">
      This screen isn't built out yet — same shell, content pending.
    </p>
  </div>
);

export default Placeholder;
